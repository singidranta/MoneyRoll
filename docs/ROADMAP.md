# 🗺️ План проекта MoneyRoll

> Главный референсный документ: что это за игра, из чего состоит, как устроен код.

---

## Что это

**MoneyRoll** — 2D онлайн-RPG в браузере, цель — **стать богатым**.
Стартуешь бомжом с $5, выполняешь мини-игры работы, делаешь бизнес, инвестируешь, скатываешься в криминал или прокачиваешь легальный путь до миллиардера. По пути встретишь «ёбнутый» неткод, из-за которого можно внезапно разориться или разбогатеть.

---

## Пиллары дизайна

1. **Цикл «работа → деньги → инвестиции → больше работы»** — главная петля.
2. **Живая экономика** — цены плавают, зависят от действий игроков.
3. **Много разных работ и магазинов** — каждая работа = отдельная мини-игра.
4. **«Ёбнутый» неткод** — задержки, race condition, пакеты-лот, биржевые манипуляции.
5. **Кастомные аватарки** — paper-doll конструктор.

---

## Список работ (расширяемый)

| ID | Название | Мини-игра | Статус |
|---|---|---|---|
| `glass-collector` | Сборщик бутылок | собрать бутылки, вернуть в киоск | 🚧 в разработке |
| `courier` | Курьер | маршрут + time-attack | 📋 следующая |
| `lemonade` | Лимонад-стенд | ритм-игра | 📋 позже |
| `trash-sort` | Мусорщик | правильно отсортировать | 📋 позже |
| `miner` | Шахтёр | кликер с апгрейдами | 📋 позже |
| `hacker` | Хакер | QTE-сессии | 📋 позже |
| `artist` | Художник | генерирует ассеты (мета!) | 📋 позже |
| `investor` | Инвестор | биржевая мини-игра | 📋 позже |

---

## Список магазинов

| ID | Название | Что делает | Статус |
|---|---|---|---|
| `recycle-kiosk` | Пункт приёма стеклотары | принимает бутылки | 🚧 в разработке |
| `food-cart` | Ларёк с едой | восстанавливает энергию | 📋 |
| `pawn-shop` | Ломбард | покупает/продаёт предметы | 📋 |
| `suspicious-shop` | Подозрительный магазин | редкие предметы | 📋 |

---

## Типы бутылок

| ID | Имя | Цена | Где спавнится |
|---|---|---|---|
| `water` | Пластиковая вода | $0.05 | везде |
| `beer-glass` | Стекло пиво | $0.20 | у баров |
| `wine` | Вино | $1.00 | у ресторанов |
| `champagne` | Шампанское | $5.00 | случайно |
| `bordeaux-1982` | Bordeaux 1982 | $50.00 | раз в неделю, race-condition |

---

## Архитектура

```
MoneyRoll/
├── client/                       # Phaser (браузер)
│   ├── src/
│   │   ├── scenes/               # Экраны игры
│   │   ├── entities/             # Игрок, бутылки, NPC
│   │   ├── systems/              # Inventory, Economy, Netcode
│   │   └── ui/                   # HUD, конструктор аватарок
│   └── public/assets/            # Спрайты, звуки, тайлы
├── server/                       # Node.js + ws
│   ├── src/
│   │   ├── index.ts
│   │   └── World.ts              # Состояние мира + эконом
├── ai-pipeline/                  # Конвейер генерации ассетов (позже)
└── docs/                         # Документация
    ├── STYLE_PROMPTS.md          # ← как генерить картинки
    ├── COMMUNICATION.md          # ← как я общаюсь
    ├── ROADMAP.md                # ← этот файл
    └── HOW_TO_RUN.md             # ← как запустить
```

---

## Технологический стек

| Слой | Технология |
|---|---|
| Клиент | Vite + TypeScript + Phaser 3 |
| Сервер | Node.js + Express + `ws` (WebSocket) |
| AI-арт | Forge WebUI / A1111 + SDXL + LoRA |
| Удаление фона | rembg (u2net) |
| Package manager | pnpm (workspaces) |

---

## Стиль арта

**Hybrid:**
- Flat 2D (стикер-стиль) — персонажи, NPC, объекты мира
- Monoline (тонкие линии) — UI и иконки

Подробности и промпты → `docs/STYLE_PROMPTS.md`.

---

## План по этапам

### Этап 0: Скелет 🚧
- Vite + TS + Phaser init, Express + ws сервер, placeholder игрок
- WASD-движение, камера следует, WS-соединение работает

### Этап 1: Игрок и движение
- Player.ts с 4-кадровой walk-анимацией
- Idle с мини-анимацией

### Этап 2: Бутылки
- Bottle.ts + spawn-система
- Инвентарь + HUD (5/5)

### Этап 3: Пункт приёма
- Kiosk.ts + UI продажи

### Этап 4: Серверная экономика
- WS broadcast spawn/pickup/sell
- Авторитет денег на сервере

### Этап 5: AI-конвейер
- Forge + LoRA + batch генерация + rembg
- Замена placeholder-спрайтов на настоящие

### Этап 6: Paper-doll аватары
- AvatarBuilder + конструктор UI
- Hot-swap частей

### Этап 7: Мини-игра «заделай трещину»
- QTE для повреждённых бутылок

### Следующие этапы
- Курьер, Лимонад, Мусорщик, Шахтёр, Хакер
- Полный неткод (race conditions, биржи)
- PvP элементы

Каждый этап — отдельный коммит + скриншот в `docs/`.

---

## Неткод-фишки (на будущее)

- **Бутылка-призрак** — сервер отправляет координаты только ближайшему игроку.
- **Race за редкой бутылкой** — broadcast всем, кто добежал — тот забрал.
- **Биржа бутылок** — можно продать право на ещё не подобранную бутылку.
- **Пакеты-лот** — failed-пакеты оставляют «лут» на карте.

---

## Текущий статус

✅ **Этап 0:** скаффолд проекта — Vite + Phaser + Express/ws, мультиплеер, ремоут-интерполяция 20Hz.
✅ **Этап "карта":** редактор с JSON-сохранением на сервере, ground + road, цикл на клик.
✅ **Radmin VPN:** сервер и Vite слушают `0.0.0.0`, IP-адреса в консоли и HUD.
🚧 **Этап 1:** реальные спрайты игроков — по 1 из каждого paper-doll слоя.

### Сетевая модель

- WebSocket на `/ws`, проксируется Vite.
- Welcome: `{type:'welcome', id, players:[{id,x,y}]}` — текущие игроки.
- Snapshot tick: 20Hz, `{type:'snapshot', t, players:[...]}` — клиент интерполирует.
- Welcome повторяется при reconnect — буфер интерполяции клиент очищает.
- `peer-join` / `leave` — для синхронизации событий подключения/отключения.

### Карта

- Карта — `MapDocument` (version=1), хранится в `server/data/map.json`.
- Эндпоинты: `GET /api/map`, `PUT /api/map`, `GET /api/network`.
- Tile types: `ground`, `road`. Цикл при клике в редакторе. Расширение — новый тип → задать в `shared/map.ts: TILE_TYPES`, добавить цвет в `MapSystem.TILE_COLORS`.
